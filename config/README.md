# Versioned contracts

Files in this directory define scientific and execution behavior independently
of Python implementation. Their canonical JSON serialization is hashed into an
experiment ID. Production execution is prohibited while a contract contains an
unresolved blocking decision.

